[build]
builder = "dockerfile"

[deploy]
startCommand = "python start.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway Volume 持久化挂载
# 在 Railway 控制台创建 Volume 后，将挂载路径设置为 /data
# 对应环境变量 DATA_DIR=/data（已在 Dockerfile 中设为默认值）
[[volumes]]
mountPath = "/data"
